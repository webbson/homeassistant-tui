{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/tom.stevens/git/homeassistant-tui && cargo fmt && cargo clippy --all-targets --quiet 2>&1 | head -30"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_FILE_PATHS\" | grep -qE '\\.(env|token)|config/.*\\.yaml$' && echo 'Blocked: potential token/config file' && exit 1 || exit 0"
          }
        ]
      }
    ]
  }
}
